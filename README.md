# Agent API Gateway Demo

A small production-oriented reference service that exposes the same backend capability through a conventional REST API and through MCP tools.

The repository is intentionally compact: the goal is to demonstrate engineering judgment around API boundaries, authorization, idempotency, reliability and observability rather than build a large application.
