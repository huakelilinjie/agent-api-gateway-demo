HOST=127.0.0.1
PORT=3000
DEMO_READ_TOKEN=replace-with-a-random-read-token
DEMO_WRITE_TOKEN=replace-with-a-random-write-token
DEMO_TOKEN_TTL_SECONDS=86400
# Optional upstream policy check. If unset, task titles are allowed locally.
POLICY_SERVICE_URL=
POLICY_TIMEOUT_MS=1000
POLICY_MAX_ATTEMPTS=3
